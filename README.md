## HongJie - 轻量化微信认证网关

### 项目声明

> **🤖 AI 生成声明**
>
> 本项目代码的 **90% 以上由 DeepSeek 生成**，少量错误逻辑和边界问题由 **Claude 进行纠正和优化**。
>
> 作者角色：
> - 🏗️ 项目框架设计
> - ⚙️ 环境部署与配置
> - 🧪 运行测试与 Bug 反馈
> - 🔄 人机协作流程协调
>
> 这是一个典型的 **AI 辅助编程实践项目**，展示了当前大语言模型在复杂业务系统开发中的能力与局限性。通过人机协作，大幅提升了开发效率，同时保证了代码质量。

---

## 项目简介

HongJie 是一个专为**中小型网站**设计的**轻量化微信认证网关**，将**反爬虫、防扫号、用户认证、登录体系**四大功能集于一体，**可自由扩展、无缝接入新老项目**，开箱即用。

### 🎯 解决的核心痛点

| 痛点 | 传统方案 | HongJie 方案 |
|------|----------|--------------|
| **用户注册登录** | 需要开发手机号/邮箱验证、密码加密、找回密码、Session/JWT 管理等全套系统，动辄数百行代码 | **零开发**，微信扫码即登录，天然免密码 |
| **用户数据存储** | 必须存储手机号、密码哈希、盐值等敏感信息，存在数据泄露风险 | **零存储**，只记录 OpenID，无密码可泄露 |
| **GDPR/个保法合规** | 需要处理数据删除请求、隐私协议、用户同意记录等复杂流程 | **天然合规**，根本不存数据 |
| **爬虫/刷接口防护** | 需要配置 WAF、验证码、限流规则，维护成本高 | **多层防御内置**，PoW + Turnstile + 限流开箱即用 |
| **扫号攻击防护** | 需要开发登录失败锁定、IP 封禁、行为分析等 | **内置动态封禁**，暴力尝试自动拉黑 |
| **新老项目接入** | 需要改造现有认证逻辑，成本高、风险大 | **透明代理**，一行代码改动即可接入 |

### 💡 核心价值：一套系统，解决四个问题

```
┌─────────────────────────────────────────────────────────────────┐
│                        HongJie 网关                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│   │  反爬虫引擎  │    │  防扫号系统  │    │  认证登录    │        │
│   │             │    │             │    │             │        │
│   │ • PoW计算   │    │ • IP动态封禁│    │ • 微信扫码   │        │
│   │ • Turnstile │    │ • 设备指纹  │    │ • 一次性令牌 │        │
│   │ • 请求限流  │    │ • 行为日志  │    │ • 透明代理   │        │
│   └─────────────┘    └─────────────┘    └─────────────┘        │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                    统一接入层                            │   │
│   │  任何后端（Python/Go/Node/PHP）只需读取 Header          │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 🔌 自由扩展：网关层设计理念

HongJie 采用**网关层代理架构**，将认证、安全、流量管控从业务代码中完全解耦：

```
                         ┌─────────────────────────────────────┐
                         │          你可以自由扩展：            │
                         │  • 添加其他登录方式（GitHub/Google）  │
                         │  • 接入 Redis/MySQL 做持久化         │
                         │  • 添加自定义限流/风控规则           │
                         │  • 对接企业内部认证系统              │
                         └─────────────────────────────────────┘
                                              │
                                              ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   老项目 A    │      │   新项目 B    │      │   微服务 C    │      │   API 服务 D  │
│  (PHP/TP5)   │      │  (Go/Gin)    │      │  (Node.js)   │      │  (Python)    │
└──────┬───────┘      └──────┬───────┘      └──────┬───────┘      └──────┬───────┘
       │                     │                     │                     │
       │   只改一行代码       │    零改动            │    零改动            │    零改动
       │      ↓               ↓                     ↓                     ↓
       └──────────────────▶  HongJie Gateway  ◀──────────────────────────┘
                                    │
                                    │  统一认证 + 安全防护
                                    ▼
                            ┌───────────────┐
                            │   微信小程序   │
                            │   (扫码登录)   │
                            └───────────────┘
```

**扩展点说明：**

| 扩展维度 | 实现方式 | 难度 |
|----------|----------|------|
| **添加登录方式** | 在网关层新增 OAuth 回调处理 | ⭐⭐ |
| **用户数据持久化** | 替换内存存储为 Redis/MySQL | ⭐⭐ |
| **自定义风控规则** | 扩展防御层中间件 | ⭐⭐⭐ |
| **多租户支持** | 添加租户识别 Header | ⭐⭐ |
| **审计日志** | 扩展可疑日志记录器 | ⭐ |

### 🚀 无缝接入新老项目：透明代理设计

**对后端完全透明，现有代码只需改动 1 行：**

```python
# 你的老项目（PHP/TP5/Laravel/ThinkPHP）
# 原来：从 session 获取用户
$user_id = $_SESSION['user_id'];

# 现在：从 Header 获取（网关自动注入）
$user_id = $_SERVER['HTTP_X_USER_ID'];
```

```
┌─────────────────────────────────────────────────────────────────┐
│                    老项目接入流程（5 分钟）                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  步骤 1：启动 HongJie 网关                                      │
│  └─ $ docker-compose up -d                                     │
│                                                                 │
│  步骤 2：修改老项目 Nginx/Apache 配置                            │
│  └─ 将请求代理到网关（或修改 DNS 指向网关）                      │
│                                                                 │
│  步骤 3：修改老项目代码（1 行）                                  │
│  └─ session('user_id') → request.header('X-User-ID')          │
│                                                                 │
│  步骤 4：部署小程序                                             │
│  └─ 微信开发者工具上传                                          │
│                                                                 │
│  完成！原有业务逻辑完全不受影响 ✅                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**支持接入的项目类型：**

| 项目类型 | 接入方式 | 改动成本 |
|----------|----------|----------|
| **PHP 老项目** (ThinkPHP/Laravel/原生) | 修改 `$_SESSION` 为 `$_SERVER['HTTP_X_USER_ID']` | 1 行 |
| **Go 项目** (Gin/Echo/原生) | 修改 `c.Get("user_id")` 为 `c.Request.Header.Get("X-User-ID")` | 1 行 |
| **Node.js 项目** (Express/Koa) | 修改 `req.session.userId` 为 `req.headers['x-user-id']` | 1 行 |
| **Python 项目** (Flask/Django) | 修改 `session.get('user_id')` 为 `request.headers.get('X-User-ID')` | 1 行 |
| **Java 项目** (Spring Boot) | 修改 `@SessionAttribute` 为 `@RequestHeader` | 1 行 |
| **Ruby on Rails** | 修改 `session[:user_id]` 为 `request.headers['HTTP_X_USER_ID']` | 1 行 |
| **纯静态网站** | 无需改动，通过 JS 读取 Header（需后端配合） | 0 行 |

### 🏗️ 多环境部署架构

```
                                    ┌─────────────────────────────────┐
                                    │         负载均衡 (Nginx)         │
                                    └─────────────────┬───────────────┘
                                                      │
                          ┌───────────────────────────┼───────────────────────────┐
                          │                           │                           │
                          ▼                           ▼                           ▼
              ┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐
              │  HongJie Gateway 1  │    │  HongJie Gateway 2  │    │  HongJie Gateway 3  │
              │    (无状态)          │    │    (无状态)          │    │    (无状态)          │
              └──────────┬──────────┘    └──────────┬──────────┘    └──────────┬──────────┘
                         │                          │                          │
                         └──────────────────────────┼──────────────────────────┘
                                                    │
                          ┌─────────────────────────┼─────────────────────────┐
                          │                         │                         │
                          ▼                         ▼                         ▼
              ┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐
              │   后端服务 A (PHP)   │    │   后端服务 B (Go)    │    │   后端服务 C (Node)  │
              └─────────────────────┘    └─────────────────────┘    └─────────────────────┘
```

**无状态设计说明：**
- 网关本身不存储会话数据（仅内存缓存临时场景）
- 可水平扩展，任意节点故障不影响整体
- 支持 Kubernetes 部署，自动伸缩

### 📊 与传统方案对比

| 对比项 | 传统自建登录 | 第三方 OAuth | HongJie |
|--------|-------------|--------------|---------|
| 开发工作量 | 3-5 天 | 0.5 天 | **5 分钟部署** |
| 用户数据存储 | 必须 | 第三方存储 | **不存储** |
| 密码管理 | 复杂（加密/重置） | 无 | **无** |
| 反爬虫能力 | 需要额外集成 | 无 | **内置** |
| 防扫号能力 | 需要额外开发 | 无 | **内置** |
| 合规成本 | 高 | 中 | **极低** |
| 后端语言限制 | 无 | 无 | **无** |
| 用户覆盖 | 需注册 | 需对应平台账号 | **12 亿微信用户** |
| **老项目接入成本** | 重构认证模块 | 中等改动 | **1 行代码** |
| **新项目开发效率** | 重复造轮子 | 仍需对接 | **零开发** |
| **扩展性** | 耦合在业务代码 | 依赖第三方 | **网关层独立扩展** |

## 在线演示

**示例网站：** [https://zhiliaoya.cn/](https://zhiliaoya.cn/)

> 访问上述网址，点击「获取二维码」，使用微信扫码即可体验完整登录流程。
>
> *注：示例网站仅用于演示，请勿提交敏感信息。*

## 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                            │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐                │
│   │ 登录页  │ -> │ 二维码  │ -> │ 轮询状态│                │
│   └─────────┘    └─────────┘    └─────────┘                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    HongJie Gateway (Go)                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                    防御层                            │    │
│  │  IP封禁 → PoW挑战 → Turnstile → 限流 → 一次性令牌   │    │
│  │  (防爬虫) (防脚本)  (防机器人) (防刷)  (防重放)     │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                    认证层                            │    │
│  │  场景管理 → 令牌交换 → 代理转发 → Cookie管理        │    │
│  │  (扫码登录) (安全交换) (透传用户) (会话保持)        │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                  扩展层（可自由定制）                 │    │
│  │  自定义认证源 | 持久化存储 | 风控规则 | 审计日志    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                    │                    │
                    ▼                    ▼
          ┌──────────────┐      ┌──────────────┐
          │  微信小程序   │      │  后端Web服务  │
          │  (12亿用户)   │      │  (你的业务)   │
          └──────────────┘      └──────────────┘
```

## 目录结构

```
HongJie/
├── WebService/                    # 服务端代码
│   ├── docker-compose.yml         # Docker 编排文件
│   ├── static/                    # 静态资源目录
│   │   └── image/                 # 图片资源
│   │       ├── favicon.ico
│   │       ├── logo.png
│   │       └── loading.gif
│   └── app/
│       └── go/
│           ├── gateway/           # 网关服务（主程序）
│           │   ├── main.go        # 网关入口
│           │   └── static_files.json  # 静态文件配置
│           └── web/               # 示例 Web 服务
│               └── main.go        # 后端示例程序
│
└── WxApp/                         # 微信小程序代码
    ├── app.js                     # 小程序入口
    ├── app.json                   # 小程序配置
    ├── app.wxss                   # 全局样式
    ├── project.config.json        # 项目配置
    ├── sitemap.json               # 站点地图
    └── pages/
        ├── index/                 # 授权确认页
        │   ├── index.js
        │   ├── index.wxml
        │   ├── index.wxss
        │   └── index.json
        └── end/                   # 结果页
            ├── end.js
            ├── end.wxml
            ├── end.wxss
            └── end.json
```

## 核心功能

### 1. 网关服务 (Gateway)

| 功能模块 | 说明 | 解决的问题 |
|---------|------|-----------|
| **微信扫码登录** | 12亿用户覆盖，扫码即登录 | 用户注册登录 |
| **一次性动态链路** | 60秒有效期，使用即失效 | 防重放攻击 |
| **IP 动态封禁** | 5/10/20 次违规自动封禁 10m/1h/24h | 防扫号 |
| **PoW 工作量证明** | SHA-256 计算，难度16 bit | 反爬虫 |
| **Turnstile 人机验证** | Cloudflare 无感验证 | 防机器人 |
| **设备指纹限流** | IP+指纹 60秒冷却 | 防刷接口 |
| **透明代理转发** | 通过 Header 传递用户身份 | 零侵入集成 |
| **静态文件服务** | 支持缓存和 ETag | 性能优化 |

### 2. 微信小程序

| 页面 | 功能 |
|------|------|
| **授权页** | 展示登录确认/取消按钮，获取微信 code 并回调网关 |
| **结果页** | 显示登录结果，支持关闭小程序 |

## 快速开始

### 前置要求

- Go 1.21+
- Docker & Docker Compose（可选）
- 微信小程序 AppID 和 AppSecret
- 域名及 SSL 证书（Let's Encrypt 推荐）
- Cloudflare Turnstile 站点密钥（可选，不配置则跳过）

### 1. 配置修改

编辑 `WebService/app/go/gateway/main.go`：

```go
var (
    wechatAppID     = "your_wechat_appid"
    wechatAppSecret = "your_wechat_secret"
    
    certPath = "/etc/letsencrypt/live/your-domain.com/fullchain.pem"
    keyPath  = "/etc/letsencrypt/live/your-domain.com/privkey.pem"
    
    webServiceURL    = "http://web-service:80"
    staticConfigPath = "static_files.json"
    
    cfTurnstileSecret  = "your_turnstile_secret"   // 可选
    cfTurnstileSiteKey = "your_turnstile_site_key" // 可选
)
```

### 2. Docker 部署

```bash
cd WebService

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f gateway-service
```

### 3. 小程序配置

1. 在微信公众平台将小程序服务器域名配置为你的网关域名
2. 修改 `WxApp/project.config.json` 中的 AppID
3. 使用微信开发者工具上传代码

### 4. 后端接入示例（新老项目通用）

**你的现有代码只需要改一行：**

```python
# 老项目：PHP/ThinkPHP/Laravel
# 原来：从 session 获取用户
$user_id = $_SESSION['user_id'];

# 现在：从 Header 获取（网关自动注入）
$user_id = $_SERVER['HTTP_X_USER_ID'];
```

```python
# 新项目：Python Flask
# 原来：从 session 获取用户
user_id = session.get('user_id')

# 现在：从 Header 获取（网关自动注入）
user_id = request.headers.get('X-User-ID')
```

**多语言完整示例：**

```go
// Go (Gin)
userID := c.Request.Header.Get("X-User-ID")
openID := c.Request.Header.Get("X-Open-ID")
```

```javascript
// Node.js (Express)
const userId = req.headers['x-user-id'];
const openId = req.headers['x-open-id'];
```

```php
// PHP (原生)
$userId = $_SERVER['HTTP_X_USER_ID'];
$openId = $_SERVER['HTTP_X_OPEN_ID'];
```

```java
// Java (Spring Boot)
@RequestHeader(value = "X-User-ID") String userId
@RequestHeader(value = "X-Open-ID") String openId
```

```ruby
# Ruby on Rails
user_id = request.headers['HTTP_X_USER_ID']
open_id = request.headers['HTTP_X_OPEN_ID']
```

## API 接口文档

### 公开接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/login` | GET | 登录页面 |
| `/api/qr-challenge` | GET | 获取 PoW 挑战 |
| `/api/qr` | POST | 生成二维码（含完整防御） |
| `/api/status` | GET | 查询场景状态 |
| `/api/ws` | GET | SSE 状态推送 |
| `/api/exchange-token` | POST | 交换认证令牌 |

### 动态链路接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/{scene_token}` | POST | 小程序回调验证（一次性） |

### 管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/admin/suspicious` | GET | 查看可疑行为日志（仅内网） |
| `/health` | GET | 健康检查 |

## 安全机制详解

### 第一层：IP 动态封禁（防扫号）
- 根据违规次数自动升级惩罚：5次→10分钟，10次→1小时，20次→24小时
- 定期清理过期封禁记录，无需人工干预
- **效果**：暴力破解密码？第 6 次尝试就被封 10 分钟

### 第二层：PoW 工作量证明（反爬虫）
- 每次二维码请求前需完成 SHA-256 计算，难度 16 bit
- 浏览器端平均需要计算数千次哈希（约 0.5-2 秒）
- **效果**：爬虫成本提高 1000 倍，普通用户无感知

### 第三层：Cloudflare Turnstile（防机器人）
- 可选的无感人机验证，无需用户点击
- **效果**：自动识别 headless 浏览器、自动化脚本

### 第四层：设备指纹限流（防刷接口）
- 基于 IP + 设备指纹的组合限流，60 秒冷却
- 浏览器 Canvas 指纹 + 屏幕特征 + 时区等
- **效果**：同设备/IP 高频请求自动限流

### 第五层：一次性动态链路（防重放）
- 每个二维码携带唯一令牌（32 位 hex）
- 60 秒有效期，使用即失效，无法二次使用
- **效果**：二维码截图分享无效，防止中间人重放

## 数据流时序图

```
浏览器                网关                 微信服务器              小程序              后端服务
   │                   │                      │                    │                   │
   │   GET /login      │                      │                    │                   │
   │──────────────────>│                      │                    │                   │
   │                   │                      │                    │                   │
   │   PoW挑战+二维码   │                      │                    │                   │
   │<──────────────────│                      │                    │                   │
   │                   │                      │                    │                   │
   │   POST /api/qr    │                      │                    │                   │
   │  (PoW+Turnstile)  │                      │                    │                   │
   │──────────────────>│                      │                    │                   │
   │                   │  生成scene_token      │                    │                   │
   │                   │  返回二维码+scene_id  │                    │                   │
   │<──────────────────│                      │                    │                   │
   │                   │                      │                    │                   │
   │   SSE轮询状态     │                      │  用户扫码          │                   │
   │──────────────────>│                      │<───────────────────│                   │
   │                   │                      │                    │                   │
   │                   │   POST /api/{token}   │                    │                   │
   │                   │<─────────────────────────────────────────│                   │
   │                   │   ?code=xxx          │                    │                   │
   │                   │                      │                    │                   │
   │                   │  jscode2session      │                    │                   │
   │                   │─────────────────────>│                    │                   │
   │                   │  return openid       │                    │                   │
   │                   │<─────────────────────│                    │                   │
   │                   │                      │                    │                   │
   │                   │  更新场景状态为confirmed│                    │                   │
   │                   │                      │                    │                   │
   │   状态更新(confirmed)│                    │                    │                   │
   │<──────────────────│                      │                    │                   │
   │                   │                      │                    │                   │
   │   POST /api/exchange-token               │                    │                   │
   │──────────────────>│                      │                    │                   │
   │                   │  生成auth_token       │                    │                   │
   │                   │  Set-Cookie           │                    │                   │
   │<──────────────────│                      │                    │                   │
   │                   │                      │                    │                   │
   │   后续请求(带Cookie)│                      │                    │                   │
   │──────────────────>│                      │                    │                   │
   │                   │  验证auth_token       │                    │                   │
   │                   │  代理转发 + Header    │                    │                   │
   │                   │───────────────────────────────────────────────────────────>│
   │                   │                      │                    │                   │
   │   响应            │                      │                    │                   │
   │<───────────────────────────────────────────────────────────────────────────────│
```

## 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `TZ` | 时区 | `Asia/Shanghai` |
| `GO111MODULE` | Go 模块 | `on` |
| `GOPROXY` | Go 代理 | `https://goproxy.cn,direct` |
| `SERVICE_NAME` | 服务名称 | `gateway` / `web` |

## 常见问题

### Q: 如何更新微信 access_token？
A: 网关会自动定时刷新（提前 300 秒），无需手动干预。

### Q: 小程序码的有效期是多久？
A: 二维码本身是微信生成的长期有效码，但对应的 `scene_token` 有效期为 60 秒，超时需重新获取。

### Q: 如何自定义静态文件？
A: 修改 `static_files.json` 添加路径映射，文件需放在容器内对应路径。

### Q: 后端服务如何获取用户 OpenID？
A: 网关会在代理请求中添加 `X-Open-ID` Header。

### Q: 不使用 Docker 可以部署吗？
A: 可以，直接 `go run main.go` 即可，需要确保 cert.pem 和 key.pem 路径正确。

### Q: 如何扩展支持其他登录方式（如 GitHub、Google）？
A: 在网关层新增 OAuth 回调处理，复用现有的场景管理和令牌交换逻辑，欢迎 PR。

### Q: 网关如何水平扩展？
A: 网关无状态设计，可部署多实例 + 负载均衡，会话信息存储在客户端 Cookie 中。

### Q: 示例网站 https://zhiliaoya.cn/ 稳定吗？
A: 该站点用于功能演示和技术验证，不保证 100% 可用性，建议自行部署测试。

## 技术栈

| 组件 | 技术 |
|------|------|
| 网关语言 | Go 1.21+ |
| 容器化 | Docker + Docker Compose |
| 小程序 | 微信小程序原生框架 |
| 安全 | Cloudflare Turnstile + SHA-256 PoW |
| 证书 | Let's Encrypt (自动续期) |

## AI 贡献详情

| AI 模型 | 贡献内容 | 占比 |
|---------|----------|------|
| **DeepSeek** | 核心逻辑实现、框架搭建、API 设计、前端页面、小程序代码 | ~90% |
| **Claude** | 错误逻辑修复、边界条件处理、代码优化、安全性增强 | ~10% |

**人机协作流程**：
1. 作者设计整体架构和业务流程
2. DeepSeek 生成主要代码实现
3. 作者进行环境部署和运行测试
4. 发现 Bug 后反馈给 Claude 进行修正
5. 循环迭代直至功能完善

## 适用场景

| 场景 | 适合程度 | 说明 |
|------|----------|------|
| **个人博客/技术网站** | ⭐⭐⭐⭐⭐ | 无需用户系统，扫码即评论 |
| **中小企业官网** | ⭐⭐⭐⭐⭐ | 会员专区免开发，数据零存储 |
| **SaaS 工具站** | ⭐⭐⭐⭐ | 微信用户即客户，降低注册门槛 |
| **内部管理系统** | ⭐⭐⭐⭐⭐ | 员工微信扫码登录，无需维护账号密码 |
| **API 服务** | ⭐⭐⭐⭐ | 通过 Header 传递用户身份，无状态设计 |
| **微服务架构** | ⭐⭐⭐⭐⭐ | 统一认证入口，各服务零改动 |
| **老项目改造** | ⭐⭐⭐⭐⭐ | 透明代理，1 行代码接入 |
| **高并发电商** | ⭐⭐⭐ | 需要评估性能，建议做压测 |

## License

MIT License

## 贡献指南

欢迎提交 Issue 和 Pull Request

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing`)
5. 提交 Pull Request

## 致谢

- [DeepSeek](https://deepseek.com) - 核心代码生成
- [Claude](https://anthropic.com/claude) - 代码纠错与优化
- [微信开放平台](https://developers.weixin.qq.com/) - 小程序能力支持
- [Cloudflare](https://cloudflare.com) - Turnstile 人机验证服务

---

**HongJie** - 让中小网站也能拥有企业级安全防护 🛡️

> *一次部署，解决用户登录、反爬虫、防扫号三大痛点*
>
> *零侵入接入新老项目，一行代码完成认证改造*
>
> *在线演示：[https://zhiliaoya.cn/](https://zhiliaoya.cn/)*
>
> *本项目是 AI 辅助编程的实践成果，展示了人机协作的高效开发模式*
